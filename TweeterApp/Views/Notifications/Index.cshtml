@model IEnumerable<TweeterApp.Models.NotificationModel>
<h2>Notifications</h2>

@* <ul class="list-group">
    @foreach(var notif in Model)
    {
        <li class="list-group-item" @(notif.IsRead ? "" : "fw-bold")> @notif.Message <span class="text-muted">@notif.CreatedAt.ToLocalTime()</span>
            @if (!notif.IsRead)
            {
                <form asp-action="MarkAsRead" method="post" style="display:inline;">
                    <input type="hidden" name="id" value="@notif.Id"/>
                    <button class="btn btn-sm btn-outline-seacondary">Mark as read</button>
                </form>
            }
        </li>
    }
</ul> *@
@if (!Model.Any())
{
    <p>No notifications</p>
}
else
{  
    @foreach (var notification in Model)
    {
        <div class="alert alert-light border d-flex align-items-start mb-2">
            <span class="me-2 text-warning" style="font-size:1.5rem;">🔔</span>
            <div>
                <p class="mb-1"><strong>@notification.Sender?.UserName</strong> - @notification.Message</p>
                <small class="text-musted">@notification.CreatedAt.ToLocalTime().ToString("g")</small>
            </div>
        </div>
    }
}